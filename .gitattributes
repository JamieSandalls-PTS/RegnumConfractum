* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
# Batch files want CRLF; cmd.exe stops tolerating LF the moment a script
# grows a label or a goto, and the failure is silent.
*.cmd text eol=crlf
*.bat text eol=crlf
